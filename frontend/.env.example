# Base URL of the backend (no trailing slash, no /api suffix)
# Socket.IO uses VITE_SERVER_URL with https:// — it upgrades to wss:// automatically
VITE_API_URL=https://coup-backend.onrender.com
VITE_SERVER_URL=https://coup-backend.onrender.com
